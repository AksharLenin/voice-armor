services:
  - type: web
    name: vocal-armor-api
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn backend.vocal_armor_api:app --host 0.0.0.0 --port 10000
    plan: free